<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.newsapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".main.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/screen_icon_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/screen_icon_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".activities.IntroActivity"
            android:theme="@style/AppThemeNoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.newsapp.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <activity
            android:name=".activities.Bookmark"
            android:launchMode="standard"></activity>
        <activity
            android:name=".activities.OTP"
            android:launchMode="standard"></activity>
        <activity
            android:name=".activities.SearchResult"
            android:launchMode="standard"
            android:windowSoftInputMode="adjustNothing"></activity>
        <activity
            android:name=".activities.Search"
            android:launchMode="standard"
            android:windowSoftInputMode="adjustResize"></activity>
        <activity
            android:name=".activities.DetailScreen"
            android:launchMode="standard"></activity>
        <activity android:name=".activities.NewsDetail" />
        <activity
            android:name=".activities.Profile"
            android:launchMode="standard"></activity>
        <activity
            android:name=".activities.UploadStory"
            android:launchMode="standard">
            <intent-filter>
                <action android:name="android.intent.action.GET_CONTENT" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.Inquiry"
            android:launchMode="standard"
            android:windowSoftInputMode="adjustResize"></activity>
        <activity
            android:name=".activities.Signup"
            android:launchMode="standard">
            <!--            <intent-filter>-->
            <!--                <action android:name="android.intent.action.MAIN" />-->

            <!--                <category android:name="android.intent.category.LAUNCHER" />-->
            <!--            </intent-filter>-->
        </activity>
        <activity
            android:name=".activities.Home"
            android:launchMode="standard"></activity>
    </application>

</manifest>